# def add_routes(api_routes, routes, base=None):
#     for route, resource in routes:
#         if base:
#             base_route = base+route
#         else:
#             base_route = route
#         api_routes.add_resource(resource, base_route)
